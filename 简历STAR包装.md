# 医疗管家健康管理系统 — 简历 STAR 包装

> 本文档基于 P0 改造完成后的真实能力编写，所有亮点均可经得起面试深挖。

---

## 一、项目一句话定位

> **医疗管家健康管理系统**：基于 Spring Boot + Vue 3 的前后端分离体检预约与健康管理平台，自研实现 JWT 认证、Redis 缓存、Quartz 定时任务、AI 健康咨询等能力，覆盖管理后台与移动端双端 20+ 功能页面。

---

## 二、技术栈（简历用）

**后端**：Java 17 · Spring Boot · Spring Security · MyBatis-Plus · MySQL · Redis · Quartz · JWT · POI
**前端**：Vue 3 · TypeScript · Vite · Element Plus · Pinia · ECharts
**工程化**：Maven · Docker / Docker Compose · Git · RESTful API

---

## 三、亮点 STAR 包装（面试直接背）

### 亮点 1：基于 JWT + Redis 的无状态认证体系

> **S（背景）**：项目初期登录仅用明文密码比对、无任何鉴权，所有接口裸奔，存在严重安全隐患。
> **T（任务）**：为系统建立安全、可扩展的认证授权体系，且不破坏既有账号。
> **A（行动）**：
> - 引入 Spring Security + JWT（jjwt）搭建无状态认证链路，自定义认证过滤器校验 `Authorization: Bearer Token`
> - 登录令牌双写 Redis：`login:token:{token}` 实现**登出/改密后令牌立即失效**（服务端可控）
> - 密码改用 **BCrypt 加盐哈希**存储，并实现**历史明文密码平滑迁移**（校验通过后自动升级为 BCrypt）
> - 增加**登录失败锁定**：Redis 计数，连续失败 5 次锁定账号 10 分钟
> - 前端 axios 拦截器自动携带 Token，401 统一跳转登录页
> **R（结果）**：全站接口从"裸奔"升级为需认证访问，账号体系安全可审计；演示账号 `admin/123456` 登录后密码自动升级为 BCrypt，迁移零中断。

### 亮点 2：Redis 缓存与状态管理落地

> **S（背景）**：项目声明了 Redis 依赖但从未使用，热点数据（套餐列表）每次请求都查库。
> **T（任务）**：让 Redis 真正服务业务，提升响应性能。
> **A（行动）**：
> - 封装 `RedisUtil`（StringRedisTemplate），提供带过期时间的读写、计数、删除操作
> - 套餐列表接口**缓存 30 分钟**，新增/编辑/删除套餐时主动失效刷新，保证缓存一致性
> - Redis 承载认证状态（Token）、登录失败计数等多类数据
> **R（结果）**：套餐列表等热点接口命中缓存，减少数据库压力；Redis 从"摆设"变成认证与性能的核心组件。

### 亮点 3：Quartz 定时任务自动清理过期订单

> **S（背景）**：预约订单"预约日期已过但状态仍为未到诊"，导致数据失真、占用名额。
> **T（任务）**：定时自动修正订单状态。
> **A（行动）**：基于 Quartz 实现 `OrderExpireJob`，默认每天 01:00 扫描并批量将过期未到诊订单更新为"已过期"；Cron 表达式外部化配置，支持环境变量覆盖便于测试。
> **R（结果）**：订单状态自动收敛、名额及时释放；通过临时高频 Cron（每 5 秒）实测一次性正确更新 11 条过期订单，零人工干预。

### 亮点 4：安全配置外部化 + Docker 一键部署

> **S（背景）**：数据库密码、AI Key、JWT 密钥硬编码在 `application.yml`，存在泄露风险；无部署脚本。
> **T（任务）**：敏感信息脱敏、实现一键部署。
> **A（行动）**：
> - 全部敏感配置改为 `${ENV_VAR:默认值}` 环境变量注入，提供 `.env.example` 模板，`.gitignore` 排除 `.env`
> - 编写多阶段 `Dockerfile`（Maven 构建 + JRE 运行）+ `docker-compose.yml` 一键编排 MySQL/Redis/后端，数据库脚本随容器自动初始化
> **R（结果）**：代码库不再含任何明文密钥；一条 `docker compose up -d` 即可拉起完整环境。

### 亮点 5：AI 健康助手接入 DeepSeek 大模型

> **S（背景）**：用户需要在线健康咨询能力。
> **T（任务）**：接入大模型实现智能问答。
> **A（行动）**：对接 DeepSeek Chat API，设计医疗领域 System Prompt 约束回答边界（不做诊断、给出就医建议）；未配置 Key 时自动降级为本地关键词规则引擎兜底。
> **R（结果）**：管理端/移动端均可使用 AI 健康咨询，具备可用性兜底。

---

## 四、面试追问准备（务必掌握）

### Q1：JWT 和 Session 有什么区别？为什么用 JWT？
答：Session 服务端存储、有状态、扩展需粘性会话或共享存储；JWT 无状态、令牌自包含签名，适合前后端分离与水平扩展。本项目用 JWT + Redis 双校验，兼顾无状态与**可控失效**（登出即删）。

### Q2：JWT 的缺点？怎么应对"无法主动失效"？
答：JWT 签发后到期前无法单方面作废，存在令牌泄露风险。方案：① 缩短过期时间（本项目 2 小时）；② **Redis 保存有效令牌，登出/改密时删除**，过滤器校验 Redis 中存在性实现即时失效（本项目已实现）。

### Q3：为什么用 BCrypt 而不是 MD5/SHA？
答：MD5/SHA 是快速哈希，适合撞库和彩虹表攻击；BCrypt 内置随机盐 + 自适应慢哈希，同一密码每次加密结果不同，抗暴力破解。Spring Security 内置实现。

### Q4：Redis 缓存一致性怎么保证？
答：本项目采用**先更新数据库、后失效缓存**（Cache-Aside）：写操作删缓存，读操作未命中回源并回填。避免先删缓存导致的一段时间读到旧数据。

### Q5：登录失败锁定怎么实现的？有并发问题吗？
答：Redis `INCR` 原子自增计数，首次失败设置 10 分钟过期；达到 5 次拒绝登录。INCR 原子性避免并发下计数错乱。可进一步结合分布式锁防止恶意刷接口。

### Q6：Quartz 和 @Scheduled 的区别？
答：@Scheduled 简单、单机、不可集群；Quartz 支持 Cron 复杂调度、持久化 JobStore、集群部署、错过补偿。本项目用 Quartz 便于后续扩展调度管理页面。

### Q7：如果订单量很大，全表扫描过期订单会慢，怎么优化？
答：`t_order` 已建 `orderDate` 索引，Job 按 `orderDate < 今天 AND status=未到诊` 条件查询走索引；量大时可改为按天分批、或引入消息队列异步处理。

---

## 五、简历精简版（写进简历的 bullet）

**医疗管家健康管理系统**（个人项目）| Spring Boot · Vue 3 · MySQL · Redis · Docker
- 独立设计并实现体检预约与健康管理平台，覆盖管理后台与移动端 20+ 页面、15 张业务表
- 基于 **Spring Security + JWT + Redis** 构建无状态认证：BCrypt 密码加密、登录失败锁定、令牌登出即时失效，全站接口鉴权化
- **Redis 落地**：封装工具类实现 Token 管理、登录限流、套餐热点数据缓存（30 分钟 + 主动失效），降低数据库压力
- 基于 **Quartz** 实现订单过期定时清理任务，Cron 可配置，实测自动修正 11 条过期订单
- 对接 **DeepSeek 大模型**实现 AI 健康咨询，含本地兜底降级策略
- 完成**安全加固**：敏感配置环境变量化（数据库/密钥/AI Key 零硬编码）、**Docker Compose 一键部署**

---

## 六、避坑提醒

- ✅ 可以说：前后端分离、JWT 认证、Redis 缓存、Quartz 定时、AI 集成、Docker 部署
- ❌ 不要说：高并发、分布式、微服务、消息队列（本项目未涉及，追问会穿帮）
- ⚠️ 移动端"健康档案/健康评估"目前为前端模拟，被问及时诚实说明"移动端预约与报告查询已打通后端，健康档案模块为原型演示"
