# AGENTS.md — 前端子项目（ylgj-vue）AI 助手工作准则

> 本文件约束在本前端项目（Vue3 + TypeScript 管理后台 / 移动端）内工作的 AI 编程助手（Agent / Copilot）的协作规范。仓库根目录另有一份全局 AGENTS.md。

## 注意事项

- 每次改动完成后，都必须创建一个对应的 Git commit，以便后续追踪和回滚。
- 每次改动后，都必须编写或更新相关测试，并在交付给用户前，确保所有测试和验证全部通过（本前端至少需通过 `npx vue-tsc -b` 类型检查与浏览器冒烟）。
